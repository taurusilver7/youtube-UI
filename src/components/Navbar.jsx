import React from "react";
import styled from "styled-components";

const Container = styled.div`
  flex: 7;
`;

const Navbar = () => {
  return <Container>Navbar Component</Container>;
};

export default Navbar;
